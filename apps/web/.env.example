# Mode backend: kosong = API same-origin (wajib untuk single-service production).
# Isi URL absolut hanya untuk dev terpisah, cth. VITE_API_URL=http://localhost:3001
# Hapus baris ini = mode dummy (localStorage, tanpa backend).
VITE_API_URL=
